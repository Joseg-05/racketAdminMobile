import React from "react";
import { View, Text } from "react-native";

export const InventoryScreen = (props) => {
    return (
        <View>
            <Text>hello</Text>
        </View>
    );
};
